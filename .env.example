# Required for LLM-powered signal extraction and verdict prose.
# If unset, the app falls back to a deterministic regex extractor (lower quality).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: switch model (default: claude-sonnet-4-5)
CRACKED_MODEL=claude-sonnet-4-5
